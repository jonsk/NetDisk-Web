# pnpm 配置
#
# store-dir 必须落在**工作区之外也能写**的路径:默认的全局 store 在用户目录
# (C:\Users\...\AppData\Local\pnpm\store),在受限环境/CI 里可能不可写,
# 表现为 `EPERM: operation not permitted, mkdir ...` —— 而错误信息不会告诉你
# "是 store 位置的问题"。放在仓库内的 .pnpm-store 既自包含又便于清理。
store-dir=../.pnpm-store
# 依赖必须**精确锁定**:前端产物要进 Go 二进制,版本漂移会让"同一次 go build"
# 在不同机器上产出不同的前端(11 章:单一 tag 注入三端 —— 前提是依赖可复现)
prefer-frozen-lockfile=true
# node_modules 用硬链接(同一 store),安装更快且省磁盘
node-linker=isolated
